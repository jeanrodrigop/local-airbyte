apiVersion: v1
kind: Pod
metadata:
  name: source-postgres
  namespace: default
  labels:
    app: source-postgres
spec:
  containers:
    - name: postgres
      image: postgres:17.7-alpine
      env:
        - name: POSTGRES_USER
          value: "testuser"
        - name: POSTGRES_PASSWORD
          value: "testpassword"
        - name: POSTGRES_DB
          value: "source_db"
      ports:
        - containerPort: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: source-postgres-svc
  namespace: default
spec:
  selector:
    app: source-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432